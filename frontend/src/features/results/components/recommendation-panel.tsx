"use client";

import {
  Sofa,
  Palette,
  IndianRupee,
  Sparkles,
} from "lucide-react";

interface RecommendationPanelProps {
  furniture: string[];
  colorPalette: string[];
  estimatedCost: number;
}

export function RecommendationPanel({
  furniture,
  colorPalette,
  estimatedCost,
}: RecommendationPanelProps) {
  return (
    <section className="rounded-3xl border border-slate-200 bg-white p-8 shadow-sm dark:border-slate-700 dark:bg-slate-900">
      {/* Header */}
      <div className="mb-8 flex items-center gap-3">
        <div className="rounded-xl bg-gradient-to-br from-indigo-600 via-purple-600 to-pink-600 p-3">
          <Sparkles className="h-6 w-6 text-white" />
        </div>

        <div>
          <h2 className="text-3xl font-bold text-slate-900 dark:text-white">
            AI Recommendations
          </h2>

          <p className="text-slate-500 dark:text-slate-400">
            Personalized suggestions generated by Orvessa AI.
          </p>
        </div>
      </div>

      <div className="space-y-8">
        {/* Furniture */}
        <div>
          <div className="mb-4 flex items-center gap-2">
            <Sofa className="h-5 w-5 text-indigo-600" />

            <h3 className="text-xl font-semibold text-slate-900 dark:text-white">
              Recommended Furniture
            </h3>
          </div>

          <div className="grid gap-3">
            {furniture.map((item) => (
              <div
                key={item}
                className="rounded-2xl border border-slate-200 bg-slate-50 p-4 transition hover:shadow-md dark:border-slate-700 dark:bg-slate-800"
              >
                <span className="font-medium text-slate-900 dark:text-white">
                  {item}
                </span>
              </div>
            ))}
          </div>
        </div>

        {/* Color Palette */}
        <div>
          <div className="mb-4 flex items-center gap-2">
            <Palette className="h-5 w-5 text-pink-600" />

            <h3 className="text-xl font-semibold text-slate-900 dark:text-white">
              Suggested Color Palette
            </h3>
          </div>

          <div className="flex flex-wrap gap-4">
            {colorPalette.map((color) => (
              <div
                key={color}
                className="flex flex-col items-center"
              >
                <div
                  className="h-16 w-16 rounded-2xl border border-slate-300 shadow-sm dark:border-slate-600"
                  style={{
                    backgroundColor: color,
                  }}
                />

                <span className="mt-2 text-xs font-medium text-slate-500 dark:text-slate-400">
                  {color}
                </span>
              </div>
            ))}
          </div>
        </div>

        {/* Cost */}
        <div className="rounded-2xl bg-gradient-to-r from-indigo-600 via-purple-600 to-pink-600 p-6 text-white shadow-lg">
          <div className="flex items-center gap-3">
            <IndianRupee className="h-7 w-7" />

            <div>
              <p className="text-sm opacity-90">
                Estimated Renovation Cost
              </p>

              <h3 className="mt-1 text-4xl font-bold">
                ₹{estimatedCost.toLocaleString("en-IN")}
              </h3>
            </div>
          </div>
        </div>
      </div>
    </section>
  );
}